"""ContextLayer — the missing context layer for AI coding agents.

Index a repo's git+PR history with a multi-agent Claude pipeline, extract
structured "knowledge atoms," and serve them to any AI agent via MCP.
"""

__version__ = "0.1.0"
