"""MCP server (stdio transport) — exposes context_query + context_list_topics to Claude Code.

NOTE: this package is named `mcp_server` (not `mcp`) to avoid shadowing the
top-level `mcp` SDK from github.com/modelcontextprotocol/python-sdk.
"""
